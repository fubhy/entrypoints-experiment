{
  // NOTE: Referencing `../src` lets us import from there. The only thing we can't prevent is entrypoints importing from one another.
  "extends": "../tsconfig.base.json",
  "include": ["."],
  "references": [{ "path": "../src/tsconfig.json" }],
  "compilerOptions": {
    "composite": true,
    "rootDir": "."
  }
}
